/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com"], // Specify allowed domains for external images
  },
};

export default nextConfig;
